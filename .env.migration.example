# Supabase admin credentials for scripts (seed:dev-users, sync:user-memberships, etc.)
# Copy to .env.migration in this directory (project root) and fill in your values.
# Do not commit .env.migration (it is gitignored).

# From Supabase dashboard: your project → Settings → API
# Use the SERVICE ROLE key (secret), not the anon key.
# For seed:dev-users, use your DEV project's URL and service role key.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
